Task:
{task_description}

Context:
{task_context}

Target files (write these files — use output.files from the task contract above):
{target_files}

Attempt:
{attempt_number}

QA feedback:
{qa_feedback}

{error_history}

{existing_code}

{project_notes}

{skills_context}

{failure_patterns}

{strategy_instruction}

Implementation rules:
- Treat the task input/output/verification fields as the delivery contract
- Write the COMPLETE content of every target file listed above
- If multiple target files are listed, use the === FILE: path === header format (one header per file)
- If only one target file is listed, output just the complete file content with no header
- Keep code and files small, testable, and self-contained
- If verification.method is pytest, add or update the requested tests
- Preserve working behavior unless QA feedback requires a change

TOOLS AVAILABLE: You have Bash, Read, Write, Edit, Glob, Grep tools enabled.
- Use Read/Glob/Grep to explore existing code before implementing
- Use Bash to run `python -m pytest` or `python -c` to verify your implementation works
- Use Write/Edit to write files directly to disk
- ALWAYS end your response with the complete file content in === FILE: path === format
  so the pipeline can track your artifact, even if you already wrote it with Write tool

ARTIFACT LOCK: The PRIMARY output file MUST match the target listed above (e.g. reversi/engine.py).
Do NOT change the artifact to requirements.txt, __init__.py, or any other file type.
If the task requires a Python module, you must write that Python module.

DEPENDENCY RULE: If your implementation imports external libraries (torch, numpy, PIL, scipy, etc.),
you MUST also write a requirements.txt file listing those dependencies. Use the multi-file format:
=== FILE: reversi/engine.py ===
(your implementation here)
=== FILE: requirements.txt ===
numpy>=1.24.0
torch>=2.0.0
(etc.)

COMPLETENESS REQUIRED: Generate syntactically complete Python. Every opening ( [ { MUST have a
matching closing ) ] }. Do NOT truncate output mid-function. If the file would be very large,
implement all required methods fully — stubs with `pass` or `raise NotImplementedError` are
acceptable, but the file must parse without SyntaxError.

TEST FILE FIXES: If QA feedback says a test file has a bug (e.g. wrong assertion, wrong constructor
args, missing import in a test), you may also write the fixed test file using === FILE: tests/test_foo.py ===.
Fix only what QA specifically identified as a test bug. Do not rewrite passing tests.

Deliver the complete content of each target file so it meets the acceptance criteria.
