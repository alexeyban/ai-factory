Project description:
{project_description}

Requirements:
- Design a complete solution architecture, not just a code breakdown
- Consider backend, frontend, data, infrastructure, integrations, security, observability, and operations where relevant
- Use modern frameworks, platforms, and open-source systems when they improve the solution
- Define tasks with clear boundaries
- Include acceptance criteria
- Include dependencies
- Include architecture-oriented tasks when needed, such as API design, data model design, infra setup, messaging, deployment, or UI architecture

IMPORTANT: Each task description must be comprehensive and ready for implementation and testing. Do NOT create vague or high-level task descriptions.

Return JSON array:
[
  {{
    "task_id": "unique-task-id",
    "title": "Short descriptive title",
    "description": "Comprehensive, detailed specification of what needs to be built. Include exact requirements, inputs, outputs, edge cases, and implementation hints. This description should be sufficient for a developer to implement this task in isolation without additional clarification.",
    "type": "feature|bugfix|refactor|setup|test",
    "dependencies": ["dependency-task-id-1", "dependency-task-id-2"],
    "acceptance_criteria": ["Specific criteria 1", "Specific criteria 2", "..."],
    "estimated_size": "small|medium|large",
    "can_parallelize": true|false
  }}
]

Guidelines:
- If the project is large, create MANY small atomic tasks rather than few large ones
- Small tasks (few hours of work) are preferred over large tasks (days of work)
- Tasks with can_parallelize=true can run concurrently with other tasks
- Each task must be independently testable
