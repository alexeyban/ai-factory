Analyze this task and decompose it into atomic sub-tasks.

Task:
{task_description}

Project context:
{project_context}

Requirements:
- Break the work into tasks that are each under 8000 tokens when rendered
- Each task must include the full standard I/O interface
- Minimize dependencies and maximize parallel execution
- Each task must be independently testable
- Return JSON array only

Each task must have this shape:
{
  "task_id": "T001",
  "title": "Short descriptive title",
  "description": "Implementation-ready specification",
  "type": "feature|bugfix|refactor|setup|test|docs",
  "dependencies": [],
  "input": { "files": [], "context": "..." },
  "output": { "files": [], "artifacts": [], "expected_result": "..." },
  "verification": { "method": "pytest|manual|review", "test_file": null, "criteria": [] },
  "acceptance_criteria": [],
  "estimated_size": "small|medium|large",
  "can_parallelize": true
}
