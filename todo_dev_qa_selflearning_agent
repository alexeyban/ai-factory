Ниже — **пошаговый TODO-план адаптации self-improving agent (AlphaZero-style) под твой проект ai-factory**.
План ориентирован на production-архитектуру: расширяем существующих агентов (dev/qa) до **learning agents с памятью и skill engine**.

Я разбил на **итерации (phases)**, чтобы можно было внедрять постепенно без «переписывания всего».

---

# ✅ PHASE 0 — Базовый рефакторинг под learning-loop — DONE

## TODO

### 0.1 Unified Task Contract ✅
* [x] `normalize_task_contract()` в decomposer/agent.py
* [x] loader задач с episode_id поддержкой

### 0.2 Execution ID (эпизоды) ✅
* [x] `episode_id` на каждый запуск
* [x] `log_episode_event()` в shared/episode.py

### 0.3 Refactor Orchestrator ✅
* [x] loop execution через LearningWorkflow
* [x] N кандидатов + обратная связь через qa_feedback

---

# ✅ PHASE 1 — Memory Layer — DONE

### 1.1 Storage ✅
* [x] PostgreSQL: episodes, solutions, rewards, skills, failures
* [x] asyncpg MemoryDB client (memory/db.py)
* [x] migrations runner (memory/migrations/)

### 1.2 Episodic Memory API ✅
* [x] `store_episode()`, `get_best_solution()`, `check_regression()`

### 1.3 Failure Memory ✅
* [x] `record_failure()`, `get_failure_patterns()`, `get_failure_summary()`

### 1.4 Vector Memory ✅
* [x] Qdrant (memory/vector_store.py)
* [x] embeddings для skills и episodes

---

# ✅ PHASE 2 — Skill Engine — DONE

### 2.1–2.5 ✅
* [x] Skill dataclass (memory/skill.py)
* [x] SkillExtractor → skills/*.py + DB + Qdrant (memory/skill_extractor.py)
* [x] SkillRetriever top-K (similarity × 0.6 + success_rate × 0.4)
* [x] SkillExecutor subprocess sandbox
* [x] SkillRegistry (skills/registry.json)

---

# ✅ PHASE 3 — Dev Agent Evolution — DONE

### 3.1–3.4 ✅
* [x] Multi-candidate generation (NUM_CANDIDATES env)
* [x] Skill-aware prompting + failure patterns injection
* [x] CodeComposer: AST dedup + skill merging (orchestrator/code_composer.py)
* [x] Epsilon-greedy explore/exploit (EXPLORATION_RATE=0.3)

---

# ✅ PHASE 4 — QA + Reward System — DONE

### 4.1–4.3 ✅
* [x] RewardEngine: correctness × w_c + perf × w_p − complexity × w_x
* [x] Cyclomatic complexity via AST walk
* [x] Junit XML parsing (shared/tools.py)
* [x] Regression detection (memory/episodic.py)
* [x] Kafka publishing: qa.results + reward.computed

---

# ✅ PHASE 5 — Learning Loop (AlphaZero-like) — DONE

### 5.1–5.3 ✅
* [x] LearningWorkflow: N iterations, stagnation detection, perfect-score stop
* [x] ReplayBuffer: good/bad deques, JSON persistence (memory/replay_buffer.py)
* [x] PolicyUpdater: skill weights + prompt examples + exploration rate decay
* [x] extract_skill_activity + policy_update_activity in worker

---

# 🧬 PHASE 6 — Self-Modification (следующий этап)

## 🎯 Цель

Агент начинает **улучшать самого себя**

**Plan:** `plans/phase6_self_modification.md`

## TODO

### 6.1 Skill refactoring
* [ ] агент может переписать/оптимизировать skill через LLM
* [ ] триггер: success_rate < 0.4 после N использований

### 6.2 Skill merging
* [ ] объединение двух похожих skills в один обобщённый
* [ ] косинусное сходство > 0.85 → кандидаты на слияние

### 6.3 Skill pruning
* [ ] удаление слабых (success_rate < threshold после min_uses)
* [ ] архивирование в skills/archive/ вместо удаления

### 6.4 Meta-agent (optional)
* [ ] отдельный агент: анализирует performance метрики
* [ ] предлагает изменения: новые skills, изменение весов

---

# 🧪 PHASE 7 — Benchmarking (LeetCode pipeline)

## 🎯 Цель

Создать training environment

**Plan:** `plans/phase7_benchmarking.md`

## TODO

### 7.1 Dataset loader
* [ ] LeetCode-like задачи в JSON/YAML с тест-кейсами
* [ ] уровни сложности: easy / medium / hard

### 7.2 Curriculum learning
* [ ] easy → medium → hard по мере роста avg_reward
* [ ] автоматический переход при success_rate > 0.8

### 7.3 Metrics dashboard
* [ ] success rate по уровням
* [ ] avg reward per episode
* [ ] skill growth over time (график)

---

# ⚙️ PHASE 8 — Infra (production)

**Plan:** `plans/phase8_infra.md`

## TODO

### 8.1 Docker orchestration
* [ ] отдельные контейнеры: dev agent, qa runner, memory services
* [ ] health checks для Qdrant и PostgreSQL

### 8.2 Async execution
* [ ] Kafka очередь для skill extraction (уже частично есть)
* [ ] Redis для policy state (вместо файла)

### 8.3 Observability
* [ ] OpenTelemetry tracing per episode
* [ ] structured log aggregation per episode_id

---

# 🔥 PHASE 9 — Anti-Patterns / Stability

**Plan:** `plans/phase9_stability.md`

## TODO

### 9.1 Loop protection ✅ (частично)
* [x] max iterations (MAX_ITERATIONS)
* [x] stagnation detection (STAGNATION_THRESHOLD)

### 9.2 Reward hacking protection
* [ ] hidden test cases, недоступные dev агенту
* [ ] валидация reward через независимый QA run

### 9.3 Determinism
* [ ] RANDOM_SEED пробрасывать во все RNG (ReplayBuffer, SkillRetriever)
* [ ] reproducible LearningWorkflow runs для отладки

---

# 📦 Финальный результат
```text
Self-improving AI Factory:
- агенты учатся
- накапливают skills
- переиспользуют код
- улучшают себя
- работают как RL система
```

---

### 10.
Существенное ограничение:
использовать контракты и систему передачи сообщений с помощью текущего мехаихма через kafka

---

# 🤝 PHASE 9 — Agent Interaction Quality

## 🎯 Цель
Улучшить качество взаимодействия между PM, Architect, Analyst, Dev и QA агентами:
устранить информационные потери в пайплайне, уменьшить дублирующие LLM-вызовы,
добавить контекст PM и архитектуры в задачи для Dev-агентов.

**Plan:** `plans/phase9_agent_interactions.md`

## TODO

### 9.1 Replace PM internal full-Architect call with design briefing  [x]
* [x] Replace lines 1447-1453 in orchestrator/activities.py
* [x] Same for pm_recovery_activity lines 1677-1683
* Reduces wasted LLM call (40-50k chars → 600 words), no info loss

### 9.2 Pass architect_guidance into dev task context  [x]
* [x] architect_activity: append architect_guidance to each task's input.context after _normalize_task_list
* [x] Raise _TASK_PROJ_DESC_MAX 800 → 1200
* Dev agents now see cross-cutting standards for every task

### 9.3 Fix analyst current_state=""  [x]
* [x] orchestrator/workflows.py: pass project_goal, delivery_summary, analyst_guidance to analyst_activity
* [x] orchestrator/activities.py: build current_state from those fields instead of ""
* Analyst now knows PM goals before evaluating execution results

### 9.4 Enrich recovery failure_summary with QA context  [x]
* [x] workflows.py: add qa_root_cause, qa_fix_suggestion per failed task in failure_summary
* [x] workflows.py: pass architect_guidance + delivery_summary into recovery_request
* [x] pm_recovery_activity: use failure_summary as analyst current_state
* Recovery PM now knows WHY tasks failed, not just that they failed

### 9.5 Prompt: PM user.txt — rename input labels  [x]
* [x] "Architect input:" → "Technical design briefing (... do NOT copy as tasks):"
* [x] "Analyst input:" → "Initial risk and gap analysis:"
* Prevents PM LLM copy-paste failure mode

### 9.6 Prompt: PM system.txt — add cross-cutting standards rule  [x]
* [x] Add section 8: CROSS-CUTTING STANDARDS in architect_guidance
* architect_guidance becomes dev-actionable (naming, error handling, imports, testing)

### 9.7 Prompt: Architect user.txt — add PM-alignment instruction  [x]
* [x] Add: "do NOT add unrequested features", "keep task count proportional to PM scope"
* Prevents architect over-engineering scope creep

### 9.8 Prompt: Analyst user.txt — rename labels  [x]
* [x] "Current project state:" → "Project plan and goals:"
* [x] "New event:" → "Execution results to analyze:"
* Matches new non-empty current_state usage

### 9.9 Prompt: QA user.txt — add attempt context  [x]
* [x] Add {attempt_number} to QA prompt template
* [x] Wire attempt_number into render_prompt call in _summarize_qa_result
* QA can detect whether dev applied previous fix_suggestion on retries

---

# 🔧 PHASE 10 — E2E Pipeline Correctness

## 🎯 Цель
Починить end-to-end прогон с GLM-5: три root-cause бага блокируют все задачи.
GLM-5 оборачивает JSON в markdown code fences, обрезает ответы без max_tokens,
а QA syntax check вызывает ast.parse() на TOML/YAML/JSON файлах.

**Plan:** `plans/noble-munching-fern.md`

## TODO

### 10.1 QA syntax check — пропускать не-Python файлы  [x]
* [x] `shared/tools.py`: в `syntax_check()` добавить guard по расширению файла
* [x] Если `file_path.suffix != ".py"` → `return ToolResult(ok=True, output="syntax check skipped")`
* Исправляет: `requirements.txt` / `.yaml` / `.json` артефакты ломают QA ошибкой `ast.parse()`
* Тест добавлен: `tests/test_tools.py::test_syntax_check_skips_non_py_files`

### 10.2 Architect + PM call_llm — max_tokens=4096  [x]
* [x] `orchestrator/activities.py` `architect_activity`: добавлен `max_tokens=4096`
* [x] `pm_activity` `call_llm`: добавлен `max_tokens=4096`
* [x] `pm_recovery_activity` `call_llm`: добавлен `max_tokens=4096`
* [x] `_extract_json()` helper добавлен после `_strip_code_fences()` для разбора JSON из code fences
* Исправляет: GLM-5 обрезает JSON ответ без закрывающей ``` → fallback на 1 задачу

### 10.3 Recovery cycle condition — правильный путь к qa.status  [ ]
* [ ] `orchestrator/workflows.py` ~line 500:
  `result.get("qa_status")` → `(result.get("qa") or {}).get("status")`
* Исправляет: условие всегда возвращало None (qa вложен, не на верхнем уровне)

### 10.4 Убрать debug logging из activities.py  [ ]
* [ ] Убрать 3 временных `LOGGER.warning` для диагностики PM JSON parse (raw output, JSONDecodeError, _extract_json result)
* Оставить только оригинальную одну строку warning

### 10.5 Обновить README.md — LLM конфигурация  [ ]
* [ ] LLM_MODEL default: `opencode/bigpickle` → `glm-5`
* [ ] Debug scripts: обновить примеры с `glm-5`
* [ ] Добавить заметку о `max_tokens=4096` для PM/Architect

### 10.6 E2E верификация  [ ]
* [ ] `docker compose up -d && EXTRA_TASK_QUEUES=... python -m orchestrator.worker`
* [ ] `python scripts/e2e_simple.py` с GLM-5
* Ожидаемый результат: хотя бы 1 из 3 задач завершается `status="complete"`
